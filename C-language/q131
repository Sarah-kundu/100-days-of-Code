#include <stdio.h>

enum Days { SUNDAY, MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY };

int main() {
    enum Days d;
    for (d = SUNDAY; d <= SATURDAY; d++) {
        printf("%d\n", d);
    }

    getchar();
    getchar();
    
    return 0;
}
